interface:
  display_name: "Log Summary"
  short_description: "Дневной ClickUp-журнал работы"
  default_prompt: "Use $log-summary to create or update today's project log in ClickUp."

policy:
  allow_implicit_invocation: true
